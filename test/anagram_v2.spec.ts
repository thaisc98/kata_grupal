import { Anagramas } from '../src/app';

describe('Anagrams', () => {
  it('should return true if hola and aloh', () => {
    let input = ['hola', 'olha'];
    // // const { checkAnagrams } = new Anagramas(input);
    // expect(2).toStrictEqual(true);
    // expect(checkAnagrams('text.txt')).toStrictEqual(true);
    // expect(() => 2 + 2).toThrow('Mesaje'); for error throws
  });
});
